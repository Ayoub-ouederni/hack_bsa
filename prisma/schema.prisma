generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

// Placeholder model — full schema will be added in Step 2
model Placeholder {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
