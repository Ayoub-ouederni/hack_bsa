{
  "name": "pulse-zk-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "ZK proof circuits for Pulse community fund voting (standalone / not integrated with app)",
  "scripts": {
    "setup": "node scripts/setup.js",
    "generate-proof": "node scripts/generate_proof.js",
    "verify-proof": "node scripts/verify_proof.js",
    "full-demo": "node scripts/setup.js && node scripts/generate_proof.js && node scripts/verify_proof.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
